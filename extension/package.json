{
  "name": "sih-26171-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 (extension-scaffold): MV3 extension shell -- manifest, background service worker, offscreen inference document wired to Phase 0's confirmed Xenova/yolos-tiny + WebGPU inference call, popup, and a content-script stub for Phase 2a/3. See extension/README.md for load-unpacked steps.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/offscreen.entry.js --bundle --format=esm --platform=browser --outfile=offscreen.bundle.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.9",
    "esbuild": "^0.28.2"
  }
}
